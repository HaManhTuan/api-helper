# Quickstart — using this feature’s specifications

## 1. Read order

1. **[`spec.md`](spec.md)** — global FRs (**FR-001–FR-045**), lifecycle, locked decisions (**FR-043–045**).
2. **[`specs/README.md`](../README.md)** — index and cross-cutting notes.
3. **Role modules**: [`specs/admin/`](../admin/README.md), [`specs/customer/`](../customer/README.md), [`specs/helper/`](../helper/README.md).

## 2. Contract artifacts

- **[`contracts/README.md`](contracts/README.md)** — REST resource list and auth model summary aligned with FR-043–045.

## 3. Planning outputs in this folder

| File | Use |
|------|-----|
| `plan.md` | Implementation plan (this feature) |
| `research.md` | Resolved decisions (no open NEEDS CLARIFICATION for API binding rules) |
| `data-model.md` | Entity list for schema design |
| `tasks.md` | Generated by `/speckit.tasks` when you are ready |

## 4. Implementing elsewhere

Create an application repository (or add a `api/` tree) and map:

- **Quotes + bookings** → enforce **`quote_id`** and TTL.
- **KYC** → presigned PUT + MIME/size checks before persisting `storage_ref`.
- **Admin routes** → JWT FR-045 + permission middleware.

## 5. Constitution

All code MUST follow [`.specify/memory/constitution.md`](../../.specify/memory/constitution.md).
